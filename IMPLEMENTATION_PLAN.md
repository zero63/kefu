# 微信小程序智能客服系统 - 开发规划

> 项目代号：kefu
> 部署域名：https://kefu.xiaozhusho.top
> 数据库：MySQL 8.0（库名 kefu / 用户名 kefu / 密码 adminkefu）
> 小程序 AppID：wx6a14524a3a3c23eb
> 小程序秘钥：c3cc006a400b814f00916c388b4e29be
> 后端：**PHP 8.1+** + **Webman**（基于 Workerman 的常驻内存框架，WebSocket 一等公民）
> 前端：微信小程序（Vant Weapp）+ H5（Vue3）+ 客服工作台（Vue3 + Element Plus）

---

## 一、项目定位

本项目是一款面向中小企业的**全渠道智能云客服 SaaS 系统**。访客通过**微信小程序**（未来扩展公众号、H5、APP）发起咨询，系统提供人工在线客服、文本机器人、大模型 AI 辅助、知识库、工单、质检、数据报表等完整客服能力。

剔除"呼叫中心"模块（本项目不涉及电话坐席），其余功能模块全部纳入规划。

---

## 二、模块清单（共 9 大模块）

```
┌──────────┬──────────┬──────────┬──────────┬──────────┐
│ 1.渠道接入 │ 2.访客端  │ 3.会话流程 │ 4.客服工作台│ 5.机器人  │
└──────────┴──────────┴──────────┴──────────┴──────────┘
┌──────────┬──────────┬──────────┬──────────┐
│ 6.工单    │ 7.质检   │ 8.数据报表│ 9.系统管理│
└──────────┴──────────┴──────────┴──────────┘
```

---

## 三、模块详解（每个功能 + 为什么需要它）

### 模块 1：渠道接入（Channel）

访客从哪个入口进入客服系统，决定了产品形态。本项目**核心入口是微信小程序**，因为用户群天然在微信生态内。

| 功能 | 说明 | 为什么需要 |
|---|---|---|
| **微信小程序接入** | 通过 SDK 嵌入小程序，访客点击"客服"按钮发起会话 | 这是产品的**核心入口**，覆盖 90%+ 客户咨询场景 |
| **H5 嵌入** | 网页内嵌 JS 代码唤起客服 | 补充渠道，让商家在公众号文章、落地页也能接入客服 |
| **微信公众号** | 接收公众号粉丝的咨询消息 | 公众号粉丝基数大，是**被动咨询**的重要来源 |
| **APP 接入（预留）** | iOS/Android SDK | 为未来扩展自有 App 留接口，初期不实现 |
| **自定义消息接口** | 开放 API 让第三方系统主动推送消息到客服工作台 | 让客户的**ERP/CRM/订单系统**产生的事件（如"订单已发货"）能触发客服主动联系客户 |

---

### 模块 2：访客端（Visitor）

访客在小程序里看到的聊天界面长什么样、能发什么消息、能怎么评价，直接影响**客户体验**。

| 功能 | 说明 | 为什么需要 |
|---|---|---|
| **文本消息** | 最基础的聊天消息 | 99% 的咨询靠文字解决，**必做** |
| **图片消息** | 发送图片（截图、订单截图、商品图） | 客户常需要**截图说明问题**（如订单错误、操作报错） |
| **文件消息** | 发送 PDF/Word/Excel | B 端客户常需发送合同、报价单 |
| **语音消息** | 发送语音 | 客户**打字不便**时（比如老人、不方便打字场景） |
| **视频消息** | 发送短视频 | 客户可以**拍视频展示问题**（如商品破损、操作流程） |
| **富文本消息** | 文字+emoji+链接混合 | 让聊天内容更丰富，提升可读性 |
| **卡片消息-商品** | 商品图+标题+价格+链接 | **电商场景必备**，客户能直接看到商品并下单 |
| **卡片消息-订单** | 订单号+金额+状态+物流 | 让客服和客户对订单状态一目了然 |
| **卡片消息-线索** | 客户线索信息卡片 | 用于销售场景，**沉淀客户信息** |
| **自定义卡片-模板库** | 预设模板让客服自由组合 | 满足不同行业（教育、医疗、金融）的卡片需求 |
| **自定义卡片-代码** | 完全代码自定义 | 大客户定制化场景，**预留** |
| **表情** | 自定义 emoji | 提升聊天气氛（次要） |
| **服务时间设置** | 非工作时间提示 | 避免访客**晚上咨询没人响应**时产生投诉 |
| **询前引导语** | 客户进入时提示"请描述您的问题" | 减少**无效咨询**，让客户先说话再分配客服，节省客服时间 |
| **询前表单** | 必须填写姓名/手机/订单号才能接入 | **B 端/电商业必备**，提前收集信息减少来回沟通 |
| **欢迎语/接入提示语** | "您好，工号 1001 为您服务" | 给客户**专业感**，同时记录工号便于追责 |
| **超时安抚语** | 客服 30 秒没回复时自动发"正在为您查询，请稍等" | **降低客户焦虑感**，减少投诉 |
| **满意度评价** | 2-5 级评价 | **服务质量量化**的核心数据 |
| **评价超时回收** | 会话关闭后 24h 客户未评价，标记为"未参与"（不计入满意度分子分母） | **避免低参评率拖累数据** |
| **评价提醒推送** | 会话关闭后 1h、12h 推送评价提醒（公众号模板消息/小程序订阅消息） | **提升参评率到 30%+** |
| **评价样式** | 弹窗/气泡/自定义 | 不同行业有不同评价习惯 |

---

### 模块 3：会话流程（Session）

访客进入后，会话如何被**分配、转接、关闭、归档**——这是客服系统的**核心业务流程**。

| 功能 | 说明 | 为什么需要 |
|---|---|---|
| **会话记录查询** | 按访客/时间/客服/状态筛选会话 | **客服质检、客户投诉追溯**都依赖它 |
| **会话导出** | Excel 导出聊天记录 | 客户提出**投诉或法律纠纷**时作为证据 |
| **历史会话加载** | 老客户（6 个月内咨询过）再次进入时，客服自动看到最近 N 条历史会话摘要 | **让客服秒懂客户背景**，避免重复问 "您贵姓" |
| **会话分配-按负载** | 谁当前接待最少分配给谁 | **基础公平机制**，避免忙的忙死闲的闲死 |
| **会话分配-高级规则** | 按"上海客户→上海客服组"等自定义规则分配 | 地域化、行业化服务能力，**提升转化率** |
| **会话分配-优先进线** | VIP 客户优先 | **大客户优先服务**，提升大客户体验 |
| **会话分配-溢出规则** | A 组忙时自动转 B 组 | **保证响应率**，避免客户长时间排队 |
| **会话排队** | 客户超过接待量时排队，并显示"前面还有 X 人" | 透明化等待时间，客户可**选择等待或留言** |
| **会话排队-系统放弃** | 排队超过 N 分钟自动结束 | 节省客服时间，**避免无效等待** |
| **会话邀请** | 访客浏览网页时主动弹窗"需要帮助吗？" | **提升转化率**（电商客服关键功能） |
| **手动转接** | 客服 A 转给客服 B（专家） | 复杂问题需要**专家介入** |
| **自动转接-首回合超时** | 客服 N 秒没响应自动转给别人 | **兜底机制**，防止漏接 |
| **转接欢迎语** | "工号 1002 为您服务" | 让客户知道**换人了**，提升体验 |
| **会话关闭提示** | "会话已结束，欢迎再次咨询" | 明确会话结束边界 |
| **自定义关闭时间** | 客户 N 分钟没说话自动关闭 | 节省坐席资源，**避免无效占线** |
| **留言** | 客服全忙时客户可留言 | **非工作时间**和**高峰期**必备 |
| **留言分配** | 留言自动分配给客服组 | 保证留言**有人跟进** |
| **留言样式** | 消息流/表单两种 | 不同客户习惯不同 |
| **文本翻译** | 中英/小语种互译 | 服务**跨境客户**时需要 |

---

### 模块 4：客服工作台（Agent）

客服每天打开工作台工作 8 小时，工作台好不好用直接决定**客服效率**和**离职率**。

| 功能 | 说明 | 为什么需要 |
|---|---|---|
| **小休状态细分** | "吃饭/上厕所/培训"等细分状态 | 让**考勤数据更精确**，便于排班优化 |
| **客户标签列表提示** | 会话列表显示客户已有标签（如"VIP""已购买"） | 客服**一眼看到客户画像**，针对性服务 |
| **会话标签筛选** | 按"投诉/咨询/售后"等标签筛选会话 | **快捷定位特定类型会话** |
| **重复咨询识别** | 同一人短时间内多次咨询提示 | 提示客服**重点关注**，可能是紧急问题 |
| **关闭会话二次确认** | 关闭前弹窗确认 | 防止**客服误操作**关掉未解决问题 |
| **会话超时提醒** | 会话超过 N 分钟提示 | 防止客服**长时间挂机不结案** |
| **访客正在输入预览** | 看到访客正在打字的内容 | **提前准备答案**，提升响应速度 |
| **已读未读** | 对方是否看过消息 | 减少"客服发了但客户没看到"的争议 |
| **排队中提前进线** | 客服主动拉排队访客进线 | 提升**主动服务能力**（如发现客户咨询了很久没接） |
| **5 级咨询分类** | 咨询时选择"售前-家电-冰箱" | **数据统计**需要，统计各品类咨询量 |
| **快捷回复** | 客服输入"/dz"自动展开"亲，我们支持多种支付方式..." | **客服效率提升 50%+ 的核心功能** |
| **服务小记** | 接待后填写"问题已解决/未解决/备注" | **服务质量管理**的核心数据 |
| **自定义字段** | "订单号/金额/期望处理时间"等自定义字段 | 不同行业需要**不同的结构化信息** |
| **访客敏感词** | 客户发的"投诉""举报""曝光"等词自动**替换为同长度 \* 号**展示给客服 | 警示客服**重点关注**，同时防止访客侧输入敏感词触发平台审核 |
| **客服违禁词** | 客服发的"加微信""加群""转账""私下交易"等词**禁止发送**（前端拦截 + 服务端二次校验） | **合规管理**，避免飞单、私下交易、客户被诈骗 |
| **客服违禁词-替换模式** | 违禁词不直接拦截，而是替换为 \* 后允许发送 | 适用于"售后电话""投诉"等需要被使用但又需要留痕的词 |
| **访客超链白名单** | 客户只能发白名单内的链接 | **防钓鱼链接**，保护客户安全 |

#### 敏感词/违禁词的完整设计（为什么重要 + 怎么实现）

**为什么需要**：客服行业有两大合规风险——客户被诈骗（客服私加微信转账）、客服飞单（绕过平台私下成交）。平台必须从技术层面杜绝。同时，敏感词过滤也是平台内容合规的必要手段（网信办要求）。

**核心处理逻辑**：

```
访客发送消息"我要投诉你们"
         ↓
【1.入库前】敏感词过滤引擎扫描
         ↓
匹配到"投诉" → 替换为"**"
         ↓
入库内容为"我要**你们"
         ↓
推送给客服时展示为"我要**你们"（带高亮提示"⚠️ 命中敏感词：投诉"）
```

**两个方向的差异化处理**：

| 角色 | 命中词库后的行为 | 为什么 |
|---|---|---|
| **访客侧敏感词** | **替换为 \* 号**（不阻断），且高亮提示客服注意 | 客户是 C 端用户，平台不能粗暴拒绝沟通，但要让客服知道客户情绪 |
| **客服侧违禁词** | **三种模式可选**：① 直接拦截（消息发不出去）② 替换为 \* 号后发出（事后留痕）③ 弹窗警告但允许发送（主管模式） | 不同企业合规要求不同：金融业必须拦截，电商可警告，销售行业可留痕 |

**三个执行环节**：

1. **前端输入时**：聊天输入框实时检测，命中违禁词立即红色提示（提升客服体验）
2. **服务端入库前**：再次校验（防止绕过前端）
3. **展示渲染时**：命中 \* 号的位置用 `<span class="sensitive">` 标记，便于事后追溯

**数据库设计**：

```sql
CREATE TABLE kefu_sensitive_word (
  id INT PRIMARY KEY AUTO_INCREMENT,
  word VARCHAR(100) NOT NULL COMMENT '敏感词',
  category VARCHAR(50) COMMENT '分类：访客敏感词/客服违禁词/通用',
  level TINYINT DEFAULT 1 COMMENT '风险等级：1-低 2-中 3-高',
  action VARCHAR(20) DEFAULT 'replace' COMMENT '动作：replace替换/block拦截/warn警告',
  status TINYINT DEFAULT 1 COMMENT '1-启用 0-禁用',
  created_by INT COMMENT '创建人',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_word (word),
  INDEX idx_category (category)
) COMMENT '敏感词/违禁词库';
```

**后台管理界面要求**：
- 批量导入/导出敏感词（Excel）
- 按分类管理（广告/涉政/色情/违禁/客诉/竞品等）
- 支持**正则表达式**（如 `\d{18}` 匹配身份证号）
- 命中日志查询（哪个客服/客户、什么时候、命中什么词）
- 支持"白名单"（如"投诉"是客服常用词，可设为白名单不替换）

#### 工作台功能表（续）

| 功能 | 说明 | 为什么需要 |
|---|---|---|
| **客服最大接待量** | 同时接待 N 个会话 | 防止客服**过载导致服务质量下降** |
| **客服技能值** | "高级客服"优先分配疑难问题 | **差异化服务能力** |
| **会话协助邀请** | 客服 A 处理不了，邀请客服 B 进会话帮忙 | 复杂问题的**协作机制** |
| **访客轨迹** | 客户浏览了哪些商品 | **销售转化关键数据**（客户对哪个商品感兴趣） |
| **消息撤回** | 120s 内可撤回 | 客服**发错消息**的兜底 |
| **主动发起会话** | 客服主动联系客户（"您上次咨询的商品有优惠了"） | **主动营销/关怀** |
| **会话标记** | "稍后处理"标记 | 让客服**有序处理工作** |
| **营销助手推荐** | 根据客户画像推荐商品 | **提升客单价和转化率**（电商核心功能） |
| **AI 话术润色** | 客服输入"已发货"→ AI 扩写为"亲爱的顾客，您购买的商品已发货..." | **统一服务话术**，提升专业度 |
| **AI 智能摘要** | 长会话 AI 自动总结"客户问题：退货；解决方案：已同意" | **交接班**和**复盘**必备 |
| **AI 一键创建工单** | AI 自动生成工单内容+优先级+关键字段 | **节省客服填写工单时间** |
| **AI OCR 识别** | 客户发的图片自动提取文字 | 让客服**不用手动输入图片内容** |
| **AI 实时情绪识别** | 客户发"气死我了"自动提示客服 | **风险预警**，避免投诉升级 |
| **AI 智能会话小结** | 客服点击"小结"自动分类 | **自动化运营** |
| **AI 智能服务小记** | AI 自动从对话中提取关键信息填入字段 | **节省人工填写时间** |

---

### 模块 5：文本机器人（Robot）

机器人是**降本增效的核心**。一个训练良好的机器人能替代 60%+ 的简单咨询，让人工专注疑难问题。

| 功能 | 说明 | 为什么需要 |
|---|---|---|
| **机器人基本信息管理** | 新建/启停/编辑机器人头像名称 | 基础管理能力 |
| **数据总览** | 总会话量/有效会话量/解决率/满意度 | **运营机器人必备数据** |
| **热点知识统计** | TOP50 高频问题 | 知道客户**最关心什么**，优化知识库 |
| **知识分类统计** | 各分类的匹配次数和占比 | 知道**哪些知识有用** |
| **知识问法统计** | 每个问题的匹配/解决/采纳率 | 找出**无效知识**进行优化 |
| **转人工会话统计** | 9 种转人工方式分布 | 知道客户**为什么转人工**，针对性优化 |
| **拦截词转人工明细** | 哪些拦截词触发了转人工 | 优化拦截词配置 |
| **答案引导转人工明细** | 哪些答案引导了转人工 | 优化答案话术 |
| **一触即达路径统计** | 多轮对话各路径匹配次数 | 优化**多轮对话流程** |
| **一触即达流程统计** | 流程节点流失率 | 找到**流程瓶颈**优化 |
| **坐席辅助统计** | 机器人辅助人工的次数 | 评估**人机协作效果** |
| **相似问题学习-全量** | 列出机器人推荐但客户没采纳的问题，人工审核 | **知识库优化核心工作** |
| **相似问题学习-算法** | AI 自动聚类推荐相似问法 | **大幅提升知识库维护效率** |
| **未知问题学习** | 机器人答不上来的问题聚类 | **发现新知识**补充知识库 |
| **人工会话学习** | 从人工对话中挖掘新知识 | **冷启动**阶段必备 |
| **知识查重** | 录入时检测重复 | 避免**重复知识**浪费维护 |
| **批量知识查重** | 体检已有知识库 | **定期巡检** |
| **QA 知识抽取** | 上传 Word/PDF 自动提取 QA 对 | **冷启动利器**，1 小时做完以前 1 周的工作 |
| **机器人诊断** | 搭建期/运营期/突破期+五维度评分 | 给运营人员**明确优化方向** |
| **冷知识检测** | 找出长期没匹配的知识 | 删除/优化**无效知识** |
| **转人工前后知识关联** | 转人工前匹配什么、转人工后匹配什么 | **知识库健康度分析** |
| **单轮问答-NLP** | 自然语义理解匹配 | 机器人的**核心算法能力** |
| **一问多答** | 同一问题根据渠道/标签/时段给出不同答案 | **精准化服务** |
| **行业问法推荐** | 根据行业推荐常见问法 | **冷启动必备** |
| **批量导入/导出** | Excel 批量管理知识 | 知识库**上千条时必备** |
| **5 级问题分类** | 多级分类管理 | 知识库**结构化** |
| **答案润色-AIGC** | AI 帮写标准答案 | **降低知识库编写门槛** |
| **智能问法推荐** | AI 推荐其他问法 | **快速扩充相似问** |
| **一触即达-多轮任务** | 收集客户信息的对话流程 | **留资/查询订单/预约**等场景 |
| **客户信息存入 CRM** | 收集的信息同步到客户中心 | **线索沉淀** |
| **流程跳转** | 客户离题后能回到主流程 | 提升**多轮对话体验** |
| **复杂上下文理解** | 理解"再便宜点""那个呢"等指代 | 让对话更**自然** |
| **意图管理** | 用意图理解代替字符串匹配 | **更智能的语义识别** |
| **可视化流程画布** | 拖拽搭建多轮对话流程 | **降低运营门槛** |
| **节点管理** | 查询/对话/判断/回复/收集五种节点 | 满足**各种业务场景** |
| **变量/实体管理** | 槽位填充 | 多轮对话的**核心技术** |
| **表格知识图谱** | 用表格管理结构化知识（如"航班-出发-到达-时间"） | **查询类场景**（机票、订单、物流） |
| **自定义寒暄库** | 自定义问候/闲聊 | 让机器人更**人性化** |
| **行业知识云** | 内置电商/HR 行业知识包 | **冷启动首选** |
| **机器人测试** | 测试窗口验证匹配效果 | **上线前必备** |
| **欢迎语-分端分条件** | 网页/小程序/微信不同欢迎语 | **精准化运营** |
| **未知问题回答** | 客户问机器人答不上来的标准回复 | 兜底话术 |
| **算法语言模型** | 支持多语言 | 服务**跨境客户** |
| **常见问题-手动/智能推荐** | 客户进入时展示高频问题 | **减少咨询量**（客户自助解决） |
| **快捷短语** | 根据客户画像预判问题 | **主动服务** |
| **答案满意度/会话满意度** | 客户对机器人评价 | **机器人质量评估** |
| **沉默提醒** | 客户 N 秒没说话，机器人主动发话 | **激活沉默客户** |
| **延迟回复** | 机器人假装"正在输入" | 让客户感觉**不是机器人在敷衍** |
| **机器人转接** | 多个机器人场景，命中关键词转给另一个机器人 | **机器人协作** |
| **常驻入口转人工** | 客户随时点按钮转人工 | **基础保障** |
| **连续未知转人工** | 连续答不上来自动转人工 | **兜底服务** |
| **差评转人工** | 客户给差评自动转人工 | **挽救服务** |
| **情绪识别转人工** | 客户情绪激动自动转人工 | **风险预警** |
| **拦截词转人工** | 命中关键词直接转人工 | **紧急问题优先** |
| **排队中机器人** | 客户排队时机器人先聊 | **拦截部分排队客户** |
| **实时坐席智能辅助** | 人工对话时机器人推荐答案 | **人机协作**，提升客服效率 |
| **语音转文本** | 客户发语音机器人能识别 | **支持语音提问** |

---

### 模块 6：工单系统（Ticket）

工单是**跨部门协作的核心工具**。客服解决不了的问题、需要技术/售后/财务介入的，都通过工单流转。

| 功能 | 说明 | 为什么需要 |
|---|---|---|
| **工单创建** | 客服手动创建/机器人自动创建/AI 一键创建 | **协作起点** |
| **会话→工单流转** | 客服在会话中点击"转工单"，自动带入客户信息、聊天记录、上下文快照 | **客服解决不了的问题无缝衔接** |
| **工单受理/分配** | 分配给指定部门/人 | **责任到人** |
| **工单关联会话** | 工单详情可关联历史会话，反向也会话详情可关联历史工单 | **完整服务链路可追溯** |
| **工单流转** | 多个状态流转（待处理/处理中/已完成/已关闭） | **流程可视化** |
| **工单处理** | 添加回复/附件/服务小记 | 协作过程记录 |
| **工单完结** | 关闭+满意度评价 | **闭环管理** |
| **工单数据报表** | 工单量/SLA达成率 | **运营分析** |
| **钉钉 IM 工单** | 在钉钉侧处理工单 | 让**非客服部门**（技术、售后）也能处理 |
| **飞书 IM 工单** | 在飞书侧处理工单 | 同上 |
| **企微 IM 工单** | 在企微侧处理工单 | 同上 |
| **工单模板** | 多套工单模板（售后/投诉/建议） | **标准化流程** |
| **自定义字段** | 工单自定义字段 | **行业化定制** |
| **SLA 设置** | 响应/解决时限+超时提醒 | **服务质量保证** |
| **邮件工单** | 客户发邮件自动转工单 | **多渠道统一** |
| **邮件摘要/话术润色** | AI 辅助邮件处理 | **提升处理效率** |

---

### 模块 7：智能质检（Quality）

人工抽查 1% 的会话，无法保证服务质量。智能质检可以**100% 全量检查**。

| 功能 | 说明 | 为什么需要 |
|---|---|---|
| **关键词质检** | 检测违规关键词（如辱骂、违禁词） | **合规管理** |
| **敏感词质检** | 检测敏感信息（身份证、银行卡泄露） | **数据安全** |
| **服务规范质检** | 是否说问候语/结束语/禁用语 | **标准化服务** |
| **业务规则质检** | 是否按流程操作（报价、退换货流程） | **流程合规** |
| **多人协作质检** | 抽检/复检/申诉 | **质检公平性** |
| **质检报表** | 合格率/问题分布/排名 | **考核依据** |
| **ASR 转写（预留）** | 录音转文本（本项目暂不实现，因不含呼叫） | 为未来扩展预留 |
| **实时质检（预留）** | 通话中实时质检 | 同上 |

---

### 模块 8：数据报表（Report）

数据是**运营决策的依据**。老板每天看数据决定要不要加人，运营看数据决定要不要优化流程。

| 功能 | 说明 | 为什么需要 |
|---|---|---|
| **实时看板** | 当前咨询人数/坐席数/等待数 | **运营实时监控** |
| **客服工作量** | 接待量/响应时长/在线时长 | **排班依据** |
| **客服考勤** | 出勤/请假/迟到 | **HR 管理** |
| **排队情况** | 当前排队/分时分布 | **实时调度** |
| **排队预警** | 排队 > N 人时自动通知主管（WebSocket 推送 + 短信） | **避免客户长时间等待流失** |
| **分时咨询分布** | 时段咨询量 | **排班优化**（知道几点是高峰） |
| **历史总览** | 某段时间整体数据 | **周报月报** |
| **满意度报表** | 不同时段/类型满意度 | **服务质量评估** |
| **响应度报表** | 响应时长分布 | **SLA 监控** |
| **坐席工作量报表** | 个人工作量 | **绩效考核** |
| **坐席质检报表** | 个人质检结果 | **绩效依据** |
| **坐席考勤报表** | 个人考勤 | **工资核算** |
| **坐席服务绩效** | 评分项自动算绩效 | **考核自动化** |
| **坐席订单绩效** | 成单归属绩效（电商业） | **销售激励** |
| **留言报表** | 留言量/响应 | **非工作时间服务监控** |
| **访问统计** | 来源/关键词/着陆页 | **营销效果分析** |
| **一次性解决率** | 客户未再次来访/总会话 | **服务质量指标** |
| **应答率** | 响应时间在 X 秒内占比 | **SLA 监控** |
| **自定义报表（BI）** | BI 平台自定义报表 | **大客户定制** |
| **数据导出** | Excel/CSV/PDF | **数据二次分析** |

---

### 模块 9：系统管理（Admin）

系统能跑起来、能管好、能扩展，依赖完善的系统管理能力。

| 功能 | 说明 | 为什么需要 |
|---|---|---|
| **员工管理** | 组织架构/员工/角色权限 | **基础权限** |
| **角色权限管理** | 定义角色+对应权限 | **按岗位分配权限**（客服、主管、经理各有不同） |
| **坐席分组** | 客服组+技能组 | **按业务分配**（售前组、售后组、VIP组） |
| **客户管理** | 客户信息查询 | 客服**查看客户画像** |
| **客户标签** | 客户分类标签 | **客户分群运营** |
| **VIP 客户** | 区分 VIP | **差异化服务** |
| **黑名单** | 恶意客户禁止接入 | **防止骚扰** |
| **客户分配规则** | 新客户分配给谁 | **线索分配** |
| **企业账户管理** | 账户信息+产品服务 | **账户管理** |
| **AppKey/AppSecret** | API 密钥 | **第三方对接** |
| **自定义字段** | 系统级自定义字段 | **全局配置** |
| **操作日志** | 谁在什么时候做了什么 | **审计追溯** |
| **多语言（预留）** | 14 种访客端语言 | 服务**跨境客户** |
| **访客端样式自定义** | 自定义聊天界面 | **品牌定制** |
| **CRM 接口** | 对接企业 CRM | **数据互通** |
| **数据导出接口** | API 导出数据 | **对接 BI/数据仓库** |
| **网页/PC/移动端工作台** | 多端工作台 | **不同场景使用**（PC 办公、移动外勤） |

---

### 模块 3.10：跨模块关键设计（必须前置考虑）

这些设计**横跨所有模块**，必须在建表前就确定，否则后期改造成本巨大。

#### 3.10.1 多租户隔离（Multi-Tenancy）⭐架构级

**为什么需要**：本系统是 SaaS，要同时服务多个企业客户。A 企业的客户档案、客服账号、会话数据，**绝对不能**泄漏到 B 企业。多租户隔离是 SaaS 系统的**第一道防线**。

**三种隔离方案对比**：

| 方案 | 实现 | 优点 | 缺点 | 适用 |
|---|---|---|---|---|
| **共享数据库+tenant_id 列**（推荐） | 所有表加 `tenant_id` 字段，SQL 带 WHERE 条件 | 成本低、易迁移、易统计 | 需要严格控制 SQL（中间件强制注入） | **本项目选这个** |
| 共享数据库+Schema 隔离 | 按企业分 schema | 隔离性更强 | 跨租户统计麻烦 | 中型 SaaS |
| 独立数据库 | 每企业一个库 | 最强隔离 | 成本最高、运维复杂 | 大客户专属 |

**实施细节**：

1. **每张业务表都要有 `tenant_id` 字段**（除系统配置表、敏感词字典等全局表外）：
```sql
tenant_id BIGINT NOT NULL COMMENT '租户ID（企业ID）',
INDEX idx_tenant (tenant_id)
```

2. **中间件自动注入**：每个 HTTP/WebSocket 请求都从 token 解析出 `tenant_id`，所有 SQL 自动 `WHERE tenant_id = ?`

3. **跨租户操作严格禁止**：管理员后台的"超管"才能跨租户操作，且全部记审计日志

4. **新增 `kefu_tenant` 表**：
```sql
CREATE TABLE kefu_tenant (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  tenant_code VARCHAR(64) UNIQUE NOT NULL COMMENT '租户编码（短链）',
  tenant_name VARCHAR(200) NOT NULL COMMENT '企业名称',
  contact_name VARCHAR(50),
  contact_phone VARCHAR(20),
  contact_email VARCHAR(100),
  plan VARCHAR(20) DEFAULT 'standard' COMMENT '套餐：standard/pro/enterprise',
  status TINYINT DEFAULT 1 COMMENT '1-正常 0-禁用',
  expire_at DATETIME COMMENT '到期时间',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_code (tenant_code)
) COMMENT '租户（企业）表';
```

5. **Redis Key 也要带 tenant_id 前缀**：`kefu:t{1001}:session:xxx`

#### 3.10.2 消息送达与可靠投递

**为什么需要**：客服系统**最致命的故障**就是消息丢失——客户说"我要退款"客服没看到，结果客户投诉。WebSocket 长连接不像 HTTP，请求-响应模型没法保证送达。

**消息三态机制**：

```
发送方                      服务端                          接收方
  │                          │                              │
  │ ─── send(msg) ────────> │                              │
  │                          │ ── 写入数据库（持久化）        │
  │                          │ ── 推送给接收方 ────────────> │
  │                          │                              │
  │                          │ <─── ack(msg_id) ────────────│
  │                          │ ── 更新消息状态：已送达       │
  │                          │                              │
  │  (30s 后未 ack)           │                              │
  │                          │ ── 重试推送（最多 3 次）      │
  │                          │                              │
  │  (3 次失败)                │                              │
  │                          │ ── 标记消息状态：离线未送达    │
  │                          │ ── 接收方上线后补推           │
```

**消息状态机**：

| 状态 | 含义 | 转换条件 |
|---|---|---|
| `sending` | 发送中（已写入数据库，推送中） | 创建消息时 |
| `delivered` | 已送达（接收方在线且 ack） | 收到 ack |
| `read` | 已读 | 接收方查看消息 |
| `offline` | 离线未送达（接收方不在线） | 30s 未 ack |
| `failed` | 推送失败（重试 3 次仍失败） | 重试次数耗尽 |

**kefu_message 表必须新增字段**：

```sql
status VARCHAR(20) DEFAULT 'sending' COMMENT '消息状态',
ack_at DATETIME COMMENT '送达确认时间',
read_at DATETIME COMMENT '已读时间',
retry_count TINYINT DEFAULT 0 COMMENT '重试次数',
client_msg_id VARCHAR(64) COMMENT '客户端消息ID（用于幂等去重）',
INDEX idx_status (status),
UNIQUE KEY uk_client_msg (client_msg_id, session_id)
```

#### 3.10.3 消息顺序保证

**问题场景**：客服和访客端各自在 WebSocket 上接收消息，如果推送顺序错乱（如"先发送的消息后到达"），对话会看不懂。

**解决方案**：

1. **消息全局递增序号**：`session_sequence` 字段，每次会话内消息+1
2. **客户端按序号排序展示**，不按到达时间
3. **服务端推送时按序号排序**，避免乱序

```sql
ALTER TABLE kefu_message ADD COLUMN session_sequence BIGINT COMMENT '会话内消息序号';
```

#### 3.10.4 WebSocket 心跳与重连

**为什么需要**：移动网络下长连接经常被运营商踢掉（NAT 超时），访客端没有任何提示就被断线了，消息全部丢失。

**策略**：

| 场景 | 策略 |
|---|---|
| **客户端→服务端** | 每 30 秒发一次 ping，60 秒内没收到 pong 视为断线 |
| **服务端→客户端** | 检测到客户端 socket 关闭，立即重试推送 |
| **客户端断线重连** | 重连后立即拉取断线期间的所有消息（按 session_sequence 区间） |
| **断线提示** | 访客端显示"网络不稳定，正在重连..." |

#### 3.10.5 访客身份合并与去重

**问题场景**：同一客户先用 H5 匿名咨询，再登录小程序再次咨询，会创建两个 customer 记录，需要合并。

**合并规则**：

1. **基础合并**：通过 `phone` 匹配（同一手机号视为同人，自动合并）
2. **跨渠道合并**：通过 `email` 或业务方传入的 `user_id` 匹配
3. **手动合并**：管理员在客户管理后台手动选择两条记录合并
4. **合并策略**：保留更完整的画像，所有历史会话 `customer_id` 改写到合并后的 ID

```sql
CREATE TABLE kefu_customer_merge_log (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  from_customer_id VARCHAR(64) NOT NULL COMMENT '被合并的 ID',
  to_customer_id VARCHAR(64) NOT NULL COMMENT '合并到目标 ID',
  merged_fields JSON COMMENT '合并的字段明细',
  merged_by INT COMMENT '操作人',
  merged_at DATETIME DEFAULT CURRENT_TIMESTAMP
) COMMENT '客户合并日志';
```

#### 3.10.6 会话结束原因分类

**为什么需要**：客服关闭会话时必须选择原因，否则后期无法统计"为什么客户流失"。

**枚举值**：

| close_reason | 说明 | 统计意义 |
|---|---|---|
| `resolved` | 已解决 | 解决率指标 |
| `timeout` | 超时关闭 | 服务及时性 |
| `customer_leave` | 客户离开 | 流失分析 |
| `agent_leave` | 客服挂断 | 客服培训 |
| `transfer_ticket` | 转入工单 | 工单来源分析 |
| `transfer_other` | 转接其他客服 | 协同率分析 |
| `system_abandon` | 系统放弃排队 | 高峰分析 |
| `blacklist` | 黑名单拦截 | 风控分析 |

#### 3.10.7 内部协作机制（@同事 / 内部备注）

**为什么需要**：客服之间经常需要协同（如"A 处理的订单售后问题需要 B 的技术支持"）。

**功能**：

1. **@同事**：会话中输入 `@B` 触发邀请 B 加入会话
2. **内部备注**：客服可见、访客不可见的备注（用于交接班、跨部门沟通）
3. **会话内部 IM**：客服工作台侧边栏，单独和主管/同事私聊，不影响访客对话
4. **客服之间"求助"按钮**：一键发起会话邀请

**新增 `kefu_internal_msg` 表**：

```sql
CREATE TABLE kefu_internal_msg (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  tenant_id BIGINT NOT NULL,
  from_employee_id INT NOT NULL,
  to_employee_id INT NOT NULL,
  content TEXT NOT NULL,
  msg_type VARCHAR(20) DEFAULT 'text',
  is_read TINYINT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_to (to_employee_id, is_read)
) COMMENT '客服内部沟通消息';
```

#### 3.10.8 机器人"无客服模式"独立接待

**问题场景**：深夜客服全下班，访客进入客服系统，应该由机器人独立接待，而不是显示"暂无客服在线"。

**触发条件**：所有客服组都无人在线 + 在非服务时间。

**行为**：

1. 自动切换到机器人独立接待
2. 机器人顶部 banner 显示"当前是机器人服务，您可以问常见问题，复杂问题请留言"
3. 客户在机器人对话时可一键"留言"（留言会按 SLA 进入待跟进队列）
4. 服务时间恢复后，自动通知"人工客服已上线，请问是否切换到人工？"

#### 3.10.9 业务连续性（降级方案）

**为什么需要**：客服系统宕机时访客端不能直接 404，会让客户以为企业跑路了。

**降级策略**：

| 故障点 | 降级方案 |
|---|---|
| **WebSocket 服务宕机** | 访客端自动 fallback 到 HTTP 长轮询（每 3 秒拉一次消息） |
| **机器人服务宕机** | 提示"机器人正在维护中" + 直接转人工 |
| **数据库主库宕机** | 切到从库（只读模式），仅展示历史数据，新消息暂存 Redis |
| **文件上传失败** | 客户端转 base64 上传，服务端接收后异步转 OSS |
| **第三方 API 失败** | AI 网关自动切换备用模型；短信发送失败重试 3 次 |

#### 3.10.10 客服快捷键

| 快捷键 | 功能 |
|---|---|
| `Alt + Enter` | 发送消息 |
| `Ctrl + /` | 打开快捷回复 |
| `Ctrl + Shift + C` | 关闭当前会话 |
| `Ctrl + Shift + T` | 转接会话 |
| `Ctrl + Shift + N` | 新建工单 |
| `Ctrl + Shift + S` | 添加服务小记 |
| `Esc` | 取消当前编辑 |
| `@` | @同事 |

#### 3.10.11 访客端消息撤回

**为什么需要**：客户发错消息（发到错的会话、发了不该发的隐私信息）。

**策略**：

1. 客户端支持 2 分钟内撤回
2. 撤回后消息变 `withdrawn` 状态，对话双方都看到"客户撤回了一条消息"
3. 服务端不真正删除消息（用于质检和合规追溯），但展示为"已撤回"

#### 3.10.12 访客端断网/弱网处理

| 状态 | 表现 |
|---|---|
| **发送中** | 消息气泡显示"发送中..."转圈 |
| **已送达未读** | 消息气泡显示"✓" |
| **已读** | 消息气泡显示"✓✓" |
| **发送失败** | 红色感叹号 + "重发"按钮 |
| **断线中** | 顶部黄色横幅"网络不稳定，正在重连..." |
| **重连成功** | 顶部绿色提示"已恢复，自动补发 N 条消息" |

---

## 四、数据库表设计（共 47 张表）

```
├── 租户（1 张）
│   └── kefu_tenant                   租户（企业）表——多租户隔离
├── 用户权限（5 张）
│   ├── kefu_employee                 员工表（账号/密码/姓名/工号/部门/角色/状态）
│   ├── kefu_role                     角色表（客服/主管/经理/管理员）
│   ├── kefu_permission               权限表（功能-动作矩阵）
│   ├── kefu_role_permission          角色权限关联
│   └── kefu_dept                     组织架构表（公司-部门-小组）
├── 客户模块（5 张）
│   ├── kefu_customer                 客户表（openid/昵称/手机/标签/等级/负责人/**JSON profile**）
│   ├── kefu_customer_group           客户群表
│   ├── kefu_customer_tag             客户标签库
│   ├── kefu_customer_tag_rel         客户标签关联
│   └── kefu_blacklist                黑名单

```
├── 访客端自定义数据（2 张）⭐核心能力
│   ├── kefu_session_context          会话上下文快照（页面/订单/购物车/行为轨迹）
│   └── kefu_visitor_track            访客行为埋点（实时同步客户在 App/H5/小程序的操作）
│   # 注：客户画像 JSON 直接存在 kefu_customer.profile 字段
├── 会话模块（7 张）
│   ├── kefu_session                  会话记录（会话ID/客户/客服/渠道/状态/开始结束时间）
│   ├── kefu_message                  消息记录（发送者/类型/内容/时间/已读）
│   ├── kefu_evaluate                 满意度评价
│   ├── kefu_leave_msg                留言
│   ├── kefu_session_tag              会话标签
│   ├── kefu_service_note             服务小记（结构化记录）
│   └── kefu_quick_reply              快捷回复
├── 机器人模块（9 张）
│   ├── kefu_robot                    机器人基本信息
│   ├── kefu_knowledge                知识库 QA（标准问/答案/分类/状态）
│   ├── kefu_knowledge_similar        相似问法
│   ├── kefu_knowledge_category       问题分类
│   ├── kefu_intent                   意图
│   ├── kefu_flow                     一触即达流程
│   ├── kefu_flow_node                流程节点
│   ├── kefu_entity                   实体
│   └── kefu_chitchat                 寒暄库
├── 工单模块（3 张）
│   ├── kefu_ticket                   工单
│   ├── kefu_ticket_log               流转日志
│   └── kefu_ticket_template          工单模板
├── 质检模块（2 张）
│   ├── kefu_quality_rule             质检规则
│   └── kefu_quality_result           质检结果
├── 报表模块（3 张）
│   ├── kefu_report_daily             日报表
│   ├── kefu_agent_performance        坐席绩效
│   └── kefu_report_custom            自定义报表配置
├── 通用模块（10 张）
│   ├── kefu_config                   系统配置（KV）
│   ├── kefu_sms                      短信记录
│   ├── kefu_operation_log            操作日志
│   ├── kefu_file                     附件
│   ├── kefu_ai_log                   AI调用日志
│   ├── kefu_sensitive_word           敏感词/违禁词库
│   ├── kefu_sensitive_log            敏感词命中日志
│   ├── kefu_tenant                   租户（企业）表——多租户隔离
│   ├── kefu_internal_msg             客服内部沟通消息
│   └── kefu_customer_merge_log       客户合并日志
```

---

## 五、API 接口规划（约 140 个接口）

| 模块 | 接口数 | 关键接口 |
|---|---|---|
| 公共 | 8 | 登录/登出/上传文件/获取配置/获取字典 |
| 用户权限 | 12 | 员工 CRUD/角色 CRUD/权限分配/密码重置 |
| 客户管理 | 10 | 客户 CRUD/标签/群组/黑名单/VIP |
| 渠道接入 | 6 | 小程序登录/消息回调/微信公众号回调 |
| 访客端 | 8 | 发送消息/上传图片/提交评价 |
| 会话流程 | 12 | 会话列表/分配/转接/排队/留言/关闭 |
| 客服工作台 | 10 | 工作台配置/状态切换/服务小记 |
| 机器人 | 18 | 知识库 CRUD/测试/统计/学习 |
| 工单 | 8 | 工单 CRUD/流转/分配/SLA |
| 质检 | 6 | 规则配置/抽检/评分 |
| 数据报表 | 10 | 实时/历史/导出 |
| 系统管理 | 10 | 配置/日志/字典 |

---

## 六、5 期开发路线图（总计约 12-16 周）

### 第 1 期：核心骨架（P0，3 周）

**目标**：跑通"客户从小程序发起咨询 → 客服接入 → 聊天 → 评价"的最短链路

| 步骤 | 内容 | 预计时间 |
|---|---|---|
| 1.1 | 创建后端 PHP 项目骨架、数据库初始化、Redis 缓存封装 | 2 天 |
| 1.2 | 员工/角色/权限/登录/登出 | 3 天 |
| 1.3 | 客户/标签/黑名单管理 | 2 天 |
| 1.4 | 微信小程序访客端接入（SDK + 登录） | 3 天 |
| 1.5 | 客服工作台会话流程（接入/转接/关闭） | 3 天 |
| 1.6 | 满意度评价 | 1 天 |
| 1.7 | 实时数据看板 | 2 天 |

**验收**：客服登录 → 客户小程序发消息 → 客服回复 → 客户评价 → 数据看板可见

### 第 2 期：机器人 + 知识库（P0-P1，3 周）

**目标**：客户咨询时机器人先应答，答不上来自动转人工

| 步骤 | 内容 |
|---|---|
| 2.1 | 机器人基本信息管理 |
| 2.2 | 单轮问答知识库（NLP 匹配算法） |
| 2.3 | 一触即达（多轮对话基础版） |
| 2.4 | 机器人设置（欢迎语/未知回答/转人工规则） |
| 2.5 | 机器人数据报表 |

### 第 3 期：工单 + 质检（P1，2 周）

| 步骤 | 内容 |
|---|---|
| 3.1 | 工单系统（创建/流转/完结） |
| 3.2 | IM 工单（钉钉/飞书/企微） |
| 3.3 | 智能质检（关键词+规则） |
| 3.4 | 完整数据报表 |

### 第 4 期：AI 增强（P1-P2，2 周）

| 步骤 | 内容 |
|---|---|
| 4.1 | 大模型 API 集成（话术润色/摘要/OCR） |
| 4.2 | AI 一键创建工单 |
| 4.3 | AI 智能会话小结 |
| 4.4 | 知识库 AI 助手（答案润色/智能问法） |

### 第 5 期：扩展与优化（P2，2 周）

| 步骤 | 内容 |
|---|---|
| 5.1 | 微信公众号接入 |
| 5.2 | 邮件工单 |
| 5.3 | 高级报表（BI） |
| 5.4 | 多语言（预留接口） |

---

## 七、技术架构

### 7.1 后端框架选型（推荐 **Webman**）

| 候选框架 | 优点 | 缺点 | 评分 |
|---|---|---|---|
| **Webman**（强烈推荐） | 基于 Workerman 高性能常驻内存框架，单机支持 10W+ 并发；Composer 生态完整；天然支持 WebSocket/HTTP/TCP 多协议；PHP 8 协程 | 学习曲线略高于原生 | ⭐⭐⭐⭐⭐ |
| ThinkPHP 8 | 国内生态最广、文档最全、上手快 | 传统 FPM 模式，高并发下需额外优化 WebSocket | ⭐⭐⭐ |
| Laravel 11 | 生态最强、ORM 优雅、队列/任务完善 | 较重，PHP-FPM 模式下并发能力弱，部署需要 Composer/Node | ⭐⭐⭐ |
| 原生 PHP | 零依赖，部署最简单 | 路由/中间件/ORM 全部要手写，长期维护成本高 | ⭐⭐ |
| Hyperf | 协程高性能、企业级 | 学习曲线陡、文档相对少 | ⭐⭐⭐⭐ |

**为什么选 Webman（而不是 ThinkPHP/Laravel）**：

1. **WebSocket 是客服系统的命脉**：客服工作台、访客端、机器人推送、实时监控全部依赖长连接。Webman 基于 Workerman，**WebSocket 是"一等公民"**，启动即用；而 ThinkPHP/Laravel 要么用额外扩展（如 think-swoole、laravel-websocket），要么自己用 Swoole/Workerman 封装，复杂度高。
2. **性能天花板高**：单进程可处理数千并发 WebSocket 连接，1 台 4 核 8G 服务器可支撑 5W+ 同时在线用户。
3. **生态兼容**：支持 Composer、PSR-4 自动加载、PSR-15 中间件，可直接复用 TP/Laravel 的第三方包。
4. **部署友好**：一个 `webman` 可执行文件启动整个服务，无需 php-fpm/nginx-fpm 复杂配置。
5. **协议统一**：HTTP API、WebSocket、TCP（机器人 IM、企微回调）全部用同一套进程管理。

**Webman 在本项目的应用结构**：

```
server/
├── app/
│   ├── controller/
│   │   ├── visitor/        访客端控制器（小程序/H5/公众号/APP 统一入口）
│   │   ├── agent/          客服工作台控制器
│   │   ├── admin/          管理后台控制器
│   │   ├── robot/          机器人控制器
│   │   └── common/         公共接口（上传、登录、字典）
│   ├── model/              数据模型（基于 Medoo 或自研 PDO 封装）
│   ├── service/            业务逻辑层
│   ├── middleware/         中间件（鉴权、限流、日志、敏感词过滤）
│   ├── channel/            WebSocket 通道（客服端、访客端、管理端）
│   └── process/            自定义进程（消息分发、机器人推理、定时任务）
├── config/                 配置
├── route/                  路由
├── public/                 静态资源
└── vendor/                 Composer 依赖
```

### 7.2 多渠道访客端（不限微信小程序）

访客端必须**多渠道**接入，让客户无论从哪里都能找到客服。我们采用 **"统一消息总线 + 渠道适配器"** 架构：

```
                          ┌──────────────────────────┐
                          │   统一会话/消息中心        │
                          │   kefu_session / message  │
                          └──────────┬───────────────┘
                                     │
            ┌────────────────────────┼────────────────────────┐
            │                        │                        │
   ┌────────┴────────┐      ┌────────┴────────┐      ┌────────┴────────┐
   │ 微信小程序 SDK   │      │ H5 网页 JS 组件   │      │ 微信公众号       │
   │ (Channel-Weapp) │      │ (Channel-H5)    │      │ (Channel-WXOA)  │
   └─────────────────┘      └─────────────────┘      └─────────────────┘
            │                        │                        │
   ┌────────┴────────┐      ┌────────┴────────┐      ┌────────┴────────┐
   │ Android SDK     │      │ iOS SDK         │      │ 企业微信          │
   │ (Channel-APP-A) │      │ (Channel-APP-I) │      │ (Channel-Wecom) │
   └─────────────────┘      └─────────────────┘      └─────────────────┘
            │                        │                        │
   ┌────────┴────────┐      ┌────────┴────────┐      ┌────────┴────────┐
   │ 抖音小程序       │      │ 支付宝小程序      │      │ 百度营销          │
   │ (Channel-TikTok)│     │ (Channel-Alipay)│      │ (Channel-Baidu) │
   └─────────────────┘      └─────────────────┘      └─────────────────┘
```

**设计原则**：

1. **统一访客身份**：每个访客在系统中只对应一个 `customer_id`，所有渠道消息共享同一份客户档案、标签、历史会话。
2. **渠道适配器模式**：每种渠道实现统一的 `ChannelInterface` 接口（`sendMessage`、`onMessage`、`getUserInfo`），新增渠道只需写一个适配器。
3. **消息协议统一**：所有渠道的消息在内部都转成统一的 `kefu_message` 结构（sender / receiver / type / content / media_url / channel / ext_json），与渠道无关。
4. **访客端 ID 生成规则**：
   - 微信小程序：`openid`
   - 微信公众号：`openid`（与小程序隔离）
   - H5：浏览器生成的 `visitor_token`（cookie + localStorage）
   - APP：设备 ID + 用户登录 ID
   - 企业微信：外部联系人 ID

**渠道优先级（MVP 顺序）**：

| 阶段 | 渠道 | 优先级 | 说明 |
|---|---|---|---|
| MVP 第 1 期 | **微信小程序** | P0 | 主力渠道，先跑通主链路 |
| MVP 第 1 期 | **H5 网页** | P0 | 复用率高，复用小程序组件 |
| MVP 第 2 期 | **微信公众号** | P1 | 公众号粉丝咨询 |
| MVP 第 3 期 | **企业微信（外部联系人）** | P1 | B 端客户高频使用 |
| MVP 第 4 期 | **APP（iOS/Android）** | P2 | 后期自有 App 时接入 |
| 预留 | **抖音/支付宝/百度小程序** | P3 | 按客户需求扩展 |

**前端工程结构**：

```
miniprogram/                微信小程序（Vant Weapp）
h5/                         H5 访客端（Vue3 + Vant，输出 jssdk 给客户网站嵌入）
channel-sdk/
├── weapp-sdk/              微信小程序 SDK
├── h5-sdk/                 H5 SDK（输出 webchat.js）
├── wecom-sdk/              企业微信 SDK
└── docs/                   各渠道接入文档
```

**关键技术点**：

- **消息类型差异化**：小程序支持卡片消息，H5 不支持；公众号支持客服消息 48 小时限制
- **富媒体统一**：图片/文件/语音上传后存 OSS，返回 `media_url`，各渠道统一引用
- **断线重连**：所有渠道客户端实现 WebSocket 重连 + 消息幂等（按 `client_msg_id` 去重）
- **跨端会话合并**：H5 端咨询过的访客，进入小程序后看到历史会话（通过 `customer_id` 关联）

#### 7.2.1 访客端自定义数据接入（Custom User Data）⭐核心能力

**为什么需要**：客服行业有句老话——"**知道客户是谁的客服，比不知道的转化率高 3-5 倍**"。访客端通常掌握客户的关键信息（订单 ID、商品 SKU、会员等级、所在页面、购物车内容），如果这些数据能自动同步到客服工作台，客服就能**一眼看到客户的全貌**，针对性服务。

举例：
- 客户 A 进入小程序客服时，自动带上「订单号 ORD202607290001、金额 ¥299、商品名 XX 行李箱、状态 待发货」
- 客户 B 进入 H5 客服时，自动带上「当前浏览商品 SKU-8888、最近 5 次浏览记录、购物车 3 件商品」
- 客户 C 进入客服时，自动带上「VIP3、累计消费 ¥12,300、生日 7 月 30 日」

**核心数据接口设计**：

```typescript
// 访客端 SDK 初始化时传入的自定义数据
kefu.init({
  // 系统字段（必填）
  channel: 'weapp',           // 渠道标识
  visitorToken: 'xxx',        // 访客身份 token
  
  // ===== 自定义数据（核心）=====
  // 1. 用户身份信息
  user: {
    user_id: 'U12345',
    nickname: '张三',
    phone: '13800138000',
    email: 'zhangsan@example.com',
    avatar: 'https://...',
    vip_level: 3,
    register_time: '2024-01-15',
    total_spent: 12300
  },
  
  // 2. 当前上下文信息
  context: {
    page_url: '/pages/goods/detail?id=8888',
    page_title: 'XX 行李箱 - 20寸',
    page_type: 'product_detail',
    referrer: '/pages/home/index',
    device: 'iPhone 15 Pro'
  },
  
  // 3. 业务订单信息（电商业核心）
  orders: [
    { order_id: 'ORD001', amount: 299, status: '待发货', created_at: '2026-07-28' },
    { order_id: 'ORD002', amount: 1299, status: '已完成', created_at: '2026-07-15' }
  ],
  
  // 4. 商品/购物车信息
  products: [
    { sku: 'SKU-8888', name: 'XX 行李箱', price: 299, image: '...' }
  ],
  cart: [
    { sku: 'SKU-1234', name: '保温杯', qty: 2, price: 89 }
  ],
  
  // 5. 行为轨迹（最近 N 条）
  behaviors: [
    { type: 'view', target: 'SKU-8888', time: '2026-07-29 09:30' },
    { type: 'add_cart', target: 'SKU-1234', time: '2026-07-29 09:35' },
    { type: 'search', keyword: '行李箱', time: '2026-07-29 09:20' }
  ],
  
  // 6. 自定义扩展字段（业务方自由发挥）
  custom: {
    // 任何业务数据，例如：
    doctor_name: '王医生',           // 医疗行业
    course_name: '高等数学',          // 教育行业
    insurance_type: '重疾险',        // 金融行业
    hotel_booking_id: 'BK20260729001' // 酒店行业
  }
});
```

**客服工作台展示效果**：

```
┌─────────────────────────────────────────────────┐
│  当前会话                                       │
│  ┌─────────┐  张三  VIP3                          │
│  │ 头像    │  📱 13800138000                      │
│  │         │  ✉️ zhangsan@example.com             │
│  └─────────┘  💰 累计消费 ¥12,300                │
│                                                  │
│  📍 当前浏览：XX 行李箱（SKU-8888）              │
│  📦 最近订单：                                   │
│     • ORD001 ¥299 待发货（2小时前）              │
│     • ORD002 ¥1299 已完成（2周前）               │
│  🛒 购物车：保温杯 x2                            │
│  📜 行为轨迹：浏览 → 搜索 → 加购 → 进入客服       │
│                                                  │
│  🏷️ 自动标签：VIP客户 / 复购客户 / 高价值客户    │
└─────────────────────────────────────────────────┘
```

**4 个核心 API 接口**：

| 接口 | 方法 | 用途 |
|---|---|---|
| `/api/visitor/auth` | POST | 访客初始化/登录，提交 user + context |
| `/api/visitor/context/update` | POST | 访客上下文变更时同步（如切换商品页） |
| `/api/visitor/track` | POST | 访客行为埋点上报（浏览/加购/搜索） |
| `/api/visitor/event` | POST | 自定义事件上报（咨询前表单/询前问卷） |

**3 个核心设计原则**：

1. **数据归业务方所有**：访客端传什么字段完全由业务方决定，系统只负责存储+展示，**不做任何结构限制**
2. **灵活 schema**：用 JSON 字段存储（`customer.profile`、`session.context`），MySQL 8.0 原生支持 JSON 索引
3. **事件驱动**：访客端的每次关键操作（加购、下单、退出）都可以推送事件，让客服**实时感知**

**数据库补充表**：

```sql
-- 客户档案（自定义字段 JSON）
CREATE TABLE kefu_customer (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  customer_id VARCHAR(64) UNIQUE NOT NULL COMMENT '访客唯一ID',
  channel VARCHAR(32) NOT NULL COMMENT '来源渠道',
  nickname VARCHAR(100),
  phone VARCHAR(20),
  email VARCHAR(100),
  avatar VARCHAR(500),
  -- 系统字段
  vip_level TINYINT DEFAULT 0,
  total_spent DECIMAL(12,2) DEFAULT 0,
  register_time DATETIME,
  last_active_time DATETIME,
  -- 自定义 JSON 字段（业务方自由发挥）
  profile JSON COMMENT '业务自定义画像数据',
  tags JSON COMMENT '客户标签',
  -- 标准字段
  source VARCHAR(100) COMMENT '来源标识',
  status TINYINT DEFAULT 1 COMMENT '1-正常 0-黑名单',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_customer_id (customer_id),
  INDEX idx_phone (phone),
  INDEX idx_channel (channel)
) COMMENT '客户档案表';

-- 会话上下文（访客端进入客服时的场景信息）
CREATE TABLE kefu_session_context (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  session_id VARCHAR(64) NOT NULL COMMENT '会话ID',
  customer_id VARCHAR(64) NOT NULL,
  -- 系统字段
  page_url VARCHAR(500),
  page_title VARCHAR(200),
  page_type VARCHAR(50),
  referrer VARCHAR(500),
  device VARCHAR(100),
  -- 自定义上下文 JSON
  user_data JSON COMMENT '访客端传来的用户数据',
  orders JSON COMMENT '订单快照',
  products JSON COMMENT '商品快照',
  cart JSON COMMENT '购物车快照',
  behaviors JSON COMMENT '行为轨迹',
  custom JSON COMMENT '业务自定义扩展字段',
  -- 时间戳
  captured_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_session (session_id),
  INDEX idx_customer (customer_id)
) COMMENT '会话上下文快照（访客端接入时定格的客户信息）';

-- 访客行为埋点表（实时同步访客在 App/小程序/H5 的操作）
CREATE TABLE kefu_visitor_track (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  customer_id VARCHAR(64) NOT NULL,
  session_id VARCHAR(64) COMMENT '当前会话ID',
  event_type VARCHAR(50) NOT NULL COMMENT 'view/click/add_cart/search/order/pay/...',
  target VARCHAR(500) COMMENT '事件目标（SKU/页面/订单号）',
  payload JSON COMMENT '事件完整数据',
  occurred_at DATETIME NOT NULL,
  received_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_customer_time (customer_id, occurred_at),
  INDEX idx_session (session_id),
  INDEX idx_event_type (event_type)
) COMMENT '访客行为埋点';
```

**SDK 调用示例（H5 / 小程序）**：

```javascript
// H5 SDK
import Kefu from '@kefu/h5-sdk';
const kefu = new Kefu({ appKey: 'YOUR_APP_KEY' });

// 1. 用户进入商品详情页时
kefu.updateContext({
  page_url: location.href,
  page_title: document.title,
  page_type: 'product_detail',
  products: [{ sku: 'SKU-8888', name: 'XX 行李箱', price: 299 }],
  user: {
    user_id: 'U12345',
    vip_level: 3,
    total_spent: 12300
  }
});

// 2. 用户点击"加购物车"时
kefu.track('add_cart', {
  sku: 'SKU-1234',
  name: '保温杯',
  qty: 1,
  price: 89
});

// 3. 用户点击客服按钮时
kefu.openChat({
  // 可携带一次性的会话数据
  orders: getRecentOrders(),     // 业务方调用自己的订单接口
  cart: getCartItems(),          // 业务方调用自己的购物车接口
  custom: {
    last_action: 'view_product',
    intent: 'pre_sale'           // 咨询意图预判（售前/售后）
  }
});
```

**3 个应用场景**：

| 场景 | 接入的数据 | 客服收益 |
|---|---|---|
| **电商** | 订单/购物车/浏览记录/会员等级 | 一眼看到客户想买什么、买过什么、是不是 VIP，**转化率 +50%** |
| **教育** | 课程/学习进度/最近作业/考试成绩 | 针对学员弱项精准推荐课程 |
| **金融** | 持仓/收益/风险测评/产品浏览 | 合规展示客户画像，避免违规承诺收益 |
| **医疗** | 病历/医生/预约/检查报告 | 客服快速理解病情，准确分诊 |
| **SaaS** | 套餐/使用时长/工单历史/功能使用 | 精准判断客户痛点，推销升级套餐 |

**3 个数据安全原则**：

1. **敏感字段加密传输**：手机号/身份证/银行卡走 HTTPS + 字段级加密（防止中间人）
2. **按权限展示**：客服只能看自己会话的客户数据；主管看全量；管理员看所有 + 审计日志
3. **数据保留策略**：客户档案永久保留；行为轨迹保留 90 天；会话上下文保留 6 个月

### 7.3 数据层架构

```
┌─────────────────────────────────────────────────────────────┐
│                       缓存层（可选）                          │
│   本地文件缓存（初期） → Redis（后期，Webman 直接支持）         │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────┴──────────────────────────────────┐
│   Webman 业务进程                                             │
│   ├── HTTP 控制器（路由 /api/*）                              │
│   ├── WebSocket 通道（路由 /ws）                              │
│   ├── 自定义进程（消息分发 / 机器人 / 定时任务）                │
│   └── 中间件链（鉴权 → 限流 → 敏感词 → 日志 → 响应）          │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────┴──────────────────────────────────┐
│   MySQL 8.0                                                  │
│   库名 kefu                                                  │
│   - 主库（读写）                                              │
│   - 从库（后期扩展，报表查询走从库）                            │
└─────────────────────────────────────────────────────────────┘
```

### 7.4 关键设计决策

1. **后端框架固定为 Webman**（不要切换）：常驻内存 + WebSocket 一等公民 + 高并发，**客服系统刚需**
2. **访客端多渠道**：抽象 `ChannelInterface`，新增渠道只需写适配器
3. **消息表按月分表**：消息表数据量大，按月分表（`kefu_message_202601`）保证性能
4. **会话表定期归档**：超过 6 个月的会话归档到历史表（`kefu_session_archive`）
5. **文件存储**：本地存储（初期）→ OSS/COS（后期），抽象 `StorageInterface`
6. **消息分发走 Redis Stream/PubSub**（后期）：保证多进程间消息可靠分发
7. **机器人推理**：单独进程（`process/robot_worker.php`），与主进程解耦
8. **大模型调用走统一网关**：`app/service/AiGateway.php` 统一封装，便于切换模型供应商

### 7.5 完整技术栈清单

| 层级 | 技术 |
|---|---|
| **Web 服务器** | Nginx 1.22（反向代理 + 静态资源） |
| **后端框架** | **Webman**（基于 Workerman） |
| **PHP 版本** | PHP 8.1+（Webman 要求） |
| **数据库** | MySQL 8.0（库名 kefu） |
| **缓存** | 本地文件缓存 → Redis 7.x（后期） |
| **消息队列** | Redis Stream（后期，初期用进程内队列） |
| **WebSocket** | Workerman（Webman 内置） |
| **前端-小程序** | 微信原生 + Vant Weapp |
| **前端-H5** | Vue 3 + Vite + Vant 4（输出 webchat.js） |
| **前端-工作台** | Vue 3 + Element Plus + WebSocket 客户端 |
| **大模型** | 通义千问 / DeepSeek（统一 AiGateway） |
| **OCR** | 腾讯云 OCR / 百度 OCR |
| **短信** | 阿里云短信 |
| **文件存储** | 本地 → 阿里云 OSS（后期） |
| **部署** | Docker（后期） / 当前直接 phpstudy |

---

## 八、安全设计

| 风险 | 应对 |
|---|---|
| XSS 攻击 | 所有输出转义，消息内容入库前过滤 |
| SQL 注入 | 全站 PDO 预处理 |
| CSRF | API 使用 Token 鉴权 |
| 越权访问 | 每个接口做角色权限校验 |
| 客户隐私 | 手机号/身份证号入库加密（对称加密） |
| 操作审计 | 关键操作写操作日志 |
| API 限流 | 同一 IP 每分钟最多 60 次请求 |
| WebSocket 安全 | 握手时校验 token |

---

## 九、部署信息

| 项目 | 值 |
|---|---|
| 域名 | https://kefu.xiaozhusho.top |
| 后端目录 | d:\phpstudy_pro\WWW\kefu\server |
| 小程序端 | d:\phpstudy_pro\WWW\kefu\miniprogram |
| H5 端 | d:\phpstudy_pro\WWW\kefu\h5 |
| 工作台 | d:\phpstudy_pro\WWW\kefu\admin |
| 渠道 SDK | d:\phpstudy_pro\WWW\kefu\channel-sdk |
| 数据库 | MySQL 8.0（127.0.0.1:3306） |
| 缓存目录 | d:\phpstudy_pro\WWW\kefu\cache |
| 上传目录 | d:\phpstudy_pro\WWW\kefu\uploads |
| 日志目录 | d:\phpstudy_pro\WWW\kefu\logs |

### 环境要求与升级

⚠️ **PHP 版本必须升级到 8.1+**（Webman 要求）。当前 phpstudy 默认是 7.3.4，需要：

1. 下载 PHP 8.1 / 8.2 安装包（https://www.php.net/releases/）
2. phpstudy 中切换 PHP 版本到 8.1+
3. 启用扩展：`pdo_mysql`、`openssl`、`curl`、`gd`、`sockets`、`mbstring`、`bcmath`、`opcache`
4. 安装 Composer 2.x：https://getcomposer.org/Composer-Setup.exe
5. 启用 Redis（后期）/ FileCache（初期）

---

## 十、开发规范

1. **每写一个 API 接口都创建一个对应的前端测试页面**测试这个接口是否正常工作
2. **每次创建测试文件后，测试完成后修复到正常代码，不要保留测试代码**
3. **全程中文交流，代码需要中文注释**
4. **所有代码都测试通过后再交付**
5. **每个文件头注释作者、创建时间、修改记录**

---

## 十一、立即开始（第 1 期第 1 步）

按规划立即开始执行，**不需要询问确认**：

### 前置准备
1. ⚙️ 升级 PHP 到 8.1+，启用必要扩展（详见 9.1）
2. ⚙️ 安装 Composer 2.x
3. ⚙️ 确认 MySQL 已启动，数据库 `kefu` 可连接

### 第 1 期 P0 任务清单
1. ✅ 创建后端 Webman 项目骨架（`composer create-project workerman/webman server`）
2. ✅ 定义 `ChannelInterface`（多渠道适配器接口）
3. ✅ 创建数据库初始化脚本（47 张表 + 初始化测试数据）
4. ✅ 实现公共库（数据库连接、响应封装、Token 鉴权、敏感词过滤、日志）
5. ✅ 实现微信小程序 Channel 适配器（访客登录、消息收发）
6. ✅ 实现 H5 Channel 适配器（webchat.js SDK）
7. ✅ 实现登录/登出接口（管理端 + 访客端）
8. ✅ 实现客服工作台 WebSocket 通道
9. ✅ 逐个推进...